_schema-version: "3.2"
ID: ztravelab
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: ztravelab-destination-content
  type: com.sap.application.content
  requires:
  - name: ztravelab-destination-service
    parameters:
      content-target: true
  - name: ztravelab-repo-host
    parameters:
      service-key:
        name: ztravelab-repo-host-key
  - name: ztravelab-uaa
    parameters:
      service-key:
        name: ztravelab-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ztravelab_html_repo_host
          ServiceInstanceName: ztravelab-html5-service
          ServiceKeyName: ztravelab-repo-host-key
          sap.cloud.service: ztravelab
        - Authentication: OAuth2UserTokenExchange
          Name: ztravelab_uaa
          ServiceInstanceName: ztravelab-xsuaa-service
          ServiceKeyName: ztravelab-uaa-key
          sap.cloud.service: ztravelab
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: ztravelab-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ztravelab-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ztravelab.zip
      name: ztravelab
      target-path: resources/
- name: ztravelab
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ztravelab-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ztravelab-destination-service
    service-plan: lite
- name: ztravelab-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ztravelab-xsuaa-service
    service-plan: application
- name: ztravelab-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ztravelab-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
